- if object.errors.any? 
  %div
    .alert.alert-danger
      Ops! We found 
      = pluralize(object.errors.count, 'error', 'errors')
      %ul
        - object.errors.full_messages.each do |msg| 
          %li= msg